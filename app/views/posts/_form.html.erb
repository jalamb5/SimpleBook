<%= form_with(model: @post) do |form| %>
  <%= render 'posts/errors' %>
  <%= form.text_area :content, placeholder: 'Compose a new post...' %>
  <div>
    <%= form.submit id: 'btn' %>
  </div>
<% end %>
