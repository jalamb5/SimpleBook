<%= form_with(model: current_user.requested_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :requested_id, @user.id %></div>
<%= f.submit "Send Follow Request", class: "btn btn-primary" %>
<% end %>
