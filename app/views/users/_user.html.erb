<div id="<%= dom_id user %>">
  <%= "Name:  #{user.fname} #{user.lname}" %><br>
  <%= "Email: #{user.email}" %>

  <div>
    Your Posts:
    <% user.posts.each do |post| %>
      <%= render post %>
    <% end %>
  </div>

</div>
